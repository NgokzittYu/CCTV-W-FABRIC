[pytest]
markers =
    unit: fast, offline unit tests for service logic
    api: FastAPI endpoint tests with external services mocked
    integration: tests that require real external services such as IPFS or Fabric
testpaths = tests
